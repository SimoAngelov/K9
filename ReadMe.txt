K9 is an OpenGL sandbox wrapper, used to test and learn its functionality.

Prerequisites:
1) Update the system: sudo apt-get update -y
2) CMake: sudo apt-get install -y cmake
3) SDL2: sudo apt-get install libsdl2-dev

Build steps:
1) mkdir build
2) cd build
3) cmake ..
